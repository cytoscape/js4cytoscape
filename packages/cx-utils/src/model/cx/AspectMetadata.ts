type AspectMetadata = {
    name: string,
    version: string,
    idCounter: number,
    properties: [], //list of name value pairs in JSON object format.
    elementCount: number,
    consistencyGroup: number,
    checksum: string
}
